import { initializeTimes } from '../timeFunctions'

test('Renders the ReservationForm heading and checks input', () => {
    // expect(initializeTimes([])).toEqual(['17:00', '17:30', '18:00', '18:30', '19:00', '19:30', '20:30', '21:30', '22:00', '22:30', '23:00'])
})